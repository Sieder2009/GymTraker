/// Corner radii ported from `app.css` (`--r-lg`/`--r-md`/`--r-sm`) —
/// theme-independent, same in light and dark.
class AppRadii {
  static const double lg = 16;
  static const double md = 12;
  static const double sm = 8;
}
