/// Keep this in sync with `pubspec.yaml`'s `version:` field (and with the
/// `vX.Y.Z` git tag used for GitHub Releases) whenever the app is versioned
/// up — [UpdateService] compares this against the latest GitHub release.
const String kAppVersion = '1.0.0';
