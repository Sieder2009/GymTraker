<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for every network call the app makes: the live exercise
         database fetch (exercise_database_service.dart), the GitHub
         Releases update check/download (update_service.dart,
         update_provider.dart), and Health Connect's own network needs.
         The Flutter template only puts this in the debug/profile
         manifests, not here — without it, a release build (the only kind
         actually distributed via GitHub Releases) has zero network access
         and every one of those features silently no-ops. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Health Connect: read-only steps/weight sync into the app, plus
         writing finished Ironpeak workouts back out — see
         lib/services/health_service.dart. Scoped to only what's actually
         used (no location/GPS route permissions). -->
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_WEIGHT"/>
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE"/>
    <!-- NotificationService: the daily workout reminder, the guided
         workout's rest-timer ping, and the "update ready" ping (see
         lib/services/notification_service.dart). Android 13+ (API 33)
         needs POST_NOTIFICATIONS explicitly requested/granted at runtime;
         without it in the manifest there's nothing for
         requestNotificationsPermission() to actually grant, so every
         notification silently never shows. SCHEDULE_EXACT_ALARM is what
         lets the plugin's AndroidScheduleMode.exactAllowWhileIdle (used
         by both scheduled reminders) fire at the precise time on Android
         12+ instead of being silently deferred. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Lets the user actually install the .apk UpdateProvider downloads
         from GitHub Releases (see update_provider.dart, opened through
         open_filex's FileProvider). Without this, Android 8+ won't even
         offer the "install unknown apps" prompt for this app — the tap
         on "Installieren" would just silently fail. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <application
        android:label="Ironpeak Fitness"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package-visibility (Android 11+) so the app can detect whether
             the Health Connect app is installed. -->
        <package android:name="com.google.android.apps.healthdata"/>
    </queries>
</manifest>
