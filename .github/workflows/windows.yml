name: Build Windows App

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch: {} # lets us test a fix on demand without cutting a release tag

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - run: flutter pub get

      - run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - run: flutter test

      - run: flutter build windows --release

      # Ship the whole Release folder, not just the .exe — it needs its
      # sibling DLLs (flutter_windows.dll, plugin .dlls) and data/ folder
      # alongside it to actually run. UpdateProvider looks for an asset
      # whose name contains "windows" when checking GitHub Releases.
      - name: Package Windows build
        run: Compress-Archive -Path build\windows\x64\runner\Release\* -DestinationPath build\windows\x64\runner\ironpeak-fitness-windows.zip
        shell: pwsh

      - uses: actions/upload-artifact@v4
        with:
          name: ironpeak-fitness-windows
          path: mobile/build/windows/x64/runner/ironpeak-fitness-windows.zip

      - if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: mobile/build/windows/x64/runner/ironpeak-fitness-windows.zip
          generate_release_notes: true
